# Use the official MySQL image as the base image
FROM mysql:latest

# Set the root password (you may change this as needed)
ENV MYSQL_ROOT_PASSWORD=mysecretpassword

# Create a new database (you may change this as needed)
ENV MYSQL_DATABASE=example

# Expose the MySQL port (3306 by default)
EXPOSE 3306

